{
   "scripts": {
      "dev": "vercel dev",
      "build": "echo 'No build step required for static files and serverless functions'",
      "start": "vercel start"
   },
   "dependencies": {
      "@google/generative-ai": "^0.2.1"
   }
}
